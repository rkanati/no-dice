//
// no-dice
//

#include "render-stage.hpp"

namespace nd {
//  auto render_stage (const Stage& stage, Frustum& view)
//    -> std::vector<StageChunk>
//  {
//    static const auto dist_order = [] (vec3i a, vec3i b) -> bool {
//      auto a_dist = abs2 (a.chunk_pos - player_chunk);
//      auto b_dist = abs2 (b.chunk_pos - player_chunk);
//      return a_dist < b_dist;
//    };
//
//    // sort front-to-back
//    std::sort (pvs.begin (), pvs.end (), dist_order);
//
//    return pvs;
//  };
}

